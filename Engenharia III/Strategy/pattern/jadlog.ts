import { pacoteProps } from './interfaces';

export var JADLOG = function () {
    this.calcular = function (pacote: pacoteProps) {
        // calculos JADLOG...
        console.log(pacote);
        return "$39,40";
    }
};